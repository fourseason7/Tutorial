@model MVC_Login.UserList

@{
    ViewBag.Title = "UserDashBoard";
}

<h2>UserDashBoard</h2>
@if (Session["UserName"] != null)
{
    <text>
        Welcome @Session["UserName"].ToString()
    </text>
}